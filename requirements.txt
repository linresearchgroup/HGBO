# HGBO - Human-Guided Bayesian Optimization for PFAS Research
# Install with: pip install -r requirements.txt

# Core machine learning dependencies
numpy>=1.21.0
pandas>=1.3.0
scikit-learn>=1.0.0
scipy>=1.7.0
matplotlib>=3.3.0
joblib>=1.0.0

# Machine learning frameworks
xgboost>=1.6.0

# Deep learning dependencies (for MBNN)
torch>=1.12.0
torch-geometric>=2.1.0

# Chemistry and molecular informatics (for MBNN)
rdkit-pypi>=2022.9.1

# Data handling
openpyxl>=3.0.0

# Model interpretability and analysis
shap>=0.40.0                # For SHAP analysis across all models
tqdm>=4.60.0               # For progress bars in long computations

# Enhanced visualizations
seaborn>=0.11.0            # For statistical visualizations

# Jupyter notebook support
jupyter>=1.0.0             # For Jupyter notebook support
ipykernel>=6.0.0          # Jupyter kernel
notebook>=6.0.0           # Jupyter notebook interface

# Optional utilities for future enhancements
plotly>=5.0.0             # For interactive plots
kaleido>=0.2.1            # For plotly image export

# Additional scientific computing
pathlib2>=2.3.0           # Path handling utilities (Python < 3.4 compatibility)

# Note: 
# - For PyTorch installation, you may need to visit https://pytorch.org/ 
#   for GPU-specific installation instructions
# - For torch-geometric, ensure PyTorch is installed first
# - RDKit installation may require conda for some systems:
#   conda install -c conda-forge rdkit